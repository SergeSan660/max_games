# Rune//Caster migration

The repository is now the canonical development location.

Current imported prototype target: v0.9.1.

Migration plan:
1. Import the current working browser build.
2. Keep it runnable as a static site.
3. Add smoke validation before future feature work.
4. Split the single-file prototype into modules only after parity is preserved.
